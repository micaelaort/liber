@using liber.Models
@model Trivias
@{
    ViewBag.Title = "ModificarNombre";
    Layout = "~/Views/Shared/_AdminLayoutcshtml.cshtml";
}

<h2>ModificarNombre</h2>

<div id="page-wrapper">
    <div class="container-fluid">
        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    Modificar Trivia

                </h1>


                <i class="fa fa-dashboard"></i>  <a href="index.html">Dashboard</a>





                <div id="FormBanner">
                    @using (Html.BeginForm("ModificarNombre", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))

                    {
                        <h3>@ViewBag.Error </h3>
                        <header class="masthead"></header>
                        <div class="EtiquetasValidaciones">
                            <h3 id="ErrorFecha">@ViewBag.mensaje</h3>
                            <label class="etiquetas">Ingrese un nombre</label>
                        </div>
                        @Html.HiddenFor(x => x.id)
                        @Html.TextBoxFor(x => x.nombre, new { @class = "texto", placeholder = "Ingrese nombre" })
                        @Html.TextBoxFor(x => x.imagen, new { type = "file", @class = "texto", placeholder = "Ingrese imagen" })







                        <button type="submit" onclick="onsave()">Modificar Trivia</button>

                    }
                </div>


            </div>
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</div>
<!-- /#page-wrapper -->
